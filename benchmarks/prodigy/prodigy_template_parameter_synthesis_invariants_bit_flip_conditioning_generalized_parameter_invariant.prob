// @METADATA:name        = prodigy_template_parameter_synthesis_invariants_bit_flip_conditioning_generalized_parameter_invariant
// @METADATA:description = Translated from Prodigy pGCL: template_parameter_synthesis/invariants/bit_flip_conditioning_generalized_parameter_invariant.pgcl
// @METADATA:reference   = LKlinke/Prodigy (github.com/LKlinke/Prodigy)
// @METADATA:criterion   = temp
// @METADATA:expected    = unknown
// @METADATA:tags        = prodigy translated template_parameter_synthesis

if (! ( ( b1 = 0 ) && ( b2 = 0 ) )) then
  if (b3 = 0 && b4 = 0) then
    temp := i;
    i := sample_pgf ( ( ( i * p * p - i * i * p * p + 3 * i * i * p * p * p - 3 * i * i * i * p * p * p - 2 * i * i * p * p * p * p + 9 * i * i * i * p * p * p * p - 9 * i * i * i * p * p * p * p * p + 3 * i * i * i * p * p * p * p * p * p ) ) / ( i * i * p * p * p * p - 3 * i * i * p * p * p + 3 * i * i * p * p - i * i * p + i * p - i + 1 ) * ( p * p - 3 * p + 3 ) / ( p * p * ( 3 * p * p - 9 * p + 7 ) ) , i );
    i := i + temp;
    _coin :~ unif[0, 1];
    if (_coin <= p * p * ( 3 * p * p - 9 * p + 7 ) / ( p * p - 3 * p + 3 )) then
      skip;
    else
      observe(false);
    end
  else
    if (b3 = 0 || b4 = 0) then
      temp := i;
      i := sample_pgf ( i * ( p * p - i * p * p + 2 * i * p * p * p - i * p * p * p * p ) / ( i * i * p * p * p * p - 3 * i * i * p * p * p + 3 * i * i * p * p - i * i * p + i * p - i + 1 ) * ( p * p - 3 * p + 3 ) / ( ( 2 - p ) * p ) , i );
      i := i + temp;
      _coin :~ unif[0, 1];
      if (_coin <= ( 2 - p ) * p / ( p * p - 3 * p + 3 )) then
        skip;
      else
        observe(false);
      end
    else
      temp := i;
      i := sample_pgf ( 2 * i * i * ( 1 - p ) * p * p * p / ( i * i * p * p * p * p - 3 * i * i * p * p * p + 3 * i * i * p * p - i * i * p + i * p - i + 1 ) * ( p * p - 3 * p + 3 ) / ( 2 * ( 1 - p ) * p ) , i );
      i := i + temp;
      _coin :~ unif[0, 1];
      if (_coin <= 2 * ( 1 - p ) * p / ( p * p - 3 * p + 3 )) then
        skip;
      else
        observe(false);
      end
    end
  end
  b1 := 0;
  b2 := 0;
  b3 := 0;
  b4 := 0;
  temp := 0;
else
  skip;
end
return temp;